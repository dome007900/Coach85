const ids=['name','height','weight','goal','pause'];ids.forEach(id=>{const v=localStorage.getItem(id);if(v!==null)document.getElementById(id).value=v;});function update(){summary.textContent=`${name.value||'-'} • ${height.value||'-'} cm • ${weight.value||'-'} kg • Ziel: ${goal.value||'-'} • Pause: ${pause.value}s`;}update();saveProfile.onclick=()=>{['name','height','weight','goal'].forEach(id=>localStorage.setItem(id,document.getElementById(id).value));update();alert('Profil gespeichert');};saveSettings.onclick=()=>{localStorage.setItem('pause',pause.value);update();alert('Einstellungen gespeichert');};